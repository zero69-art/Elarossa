import { notFound } from "next/navigation";
import type { Metadata } from "next";
import Link from "next/link";
import { getProduct, products } from "@/lib/products";
import AddToBag from "@/components/AddToBag";
import Header from "@/components/Header";
import ProductCard from "@/components/ProductCard";
import ProductAccordions from "@/components/ProductAccordions";
import WishlistButton from "@/components/WishlistButton";

export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const { slug } = await params; const product = getProduct(slug); if (!product) return {}; return { title: product.seoTitle, description: product.metaDescription, keywords: [product.name, "women's fashion", product.category.toLowerCase(), "Elarossa"], alternates: { canonical: `/products/${product.slug}` }, openGraph: { title: product.seoTitle, description: product.metaDescription, images: [{ url: product.image, alt: `${product.name} | Elarossa` }] }, twitter: { card: "summary_large_image", title: product.seoTitle, description: product.metaDescription, images: [product.image] } }; }

export default async function ProductPage({ params }: { params: Promise<{ slug: string }> }) {
  const { slug } = await params; const product = getProduct(slug); if (!product) notFound();
  const storeLive = process.env.ELAROSSA_STORE_LIVE === "true"; const gallery = product.gallery.length ? product.gallery.slice(0, 4) : [product.image]; const related = products.filter((item) => item.slug !== product.slug && item.category === product.category).slice(0, 4); const reviewLabel = product.qualityStatus === "approved" ? "SAMPLE APPROVED" : "SAMPLE REVIEW";
  const jsonLd = { "@context": "https://schema.org", "@type": "Product", name: product.name, description: product.metaDescription, image: gallery, sku: product.slug, brand: { "@type": "Brand", name: "Elarossa" }, offers: { "@type": "Offer", priceCurrency: "USD", price: product.price.toFixed(2), availability: product.qualityStatus === "approved" && storeLive ? "https://schema.org/InStock" : "https://schema.org/PreOrder", url: `${process.env.NEXT_PUBLIC_SITE_URL || "https://elarossa.vercel.app"}/products/${product.slug}` } };

  return <main className="min-h-screen"><Header /><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }} /><div className="mx-auto max-w-7xl px-4 pb-16 sm:px-6 sm:pb-20 md:px-8"><Link href="/shop" className="mt-5 inline-flex min-h-11 items-center text-[10px] font-semibold tracking-[.18em] opacity-55 hover:opacity-100">← BACK TO EDIT</Link><section className="mt-5 grid gap-8 md:mt-8 md:grid-cols-[minmax(0,1.15fr)_minmax(340px,.85fr)] md:gap-12 lg:gap-16"><div className="grid grid-cols-2 gap-2.5 sm:gap-4">{gallery.map((image, index) => <div key={`${image}-${index}`} className={`group overflow-hidden bg-[#e9dfda] ${index === 0 ? "col-span-2" : ""}`}><img src={image} alt={`${product.name} — ${product.category} view ${index + 1}`} className={`w-full object-cover transition duration-700 group-hover:scale-[1.025] ${index === 0 ? "aspect-[4/5]" : "aspect-square"}`} loading={index === 0 ? "eager" : "lazy"} /></div>)}</div><div className="md:sticky md:top-24 md:h-fit"><div className="flex items-center justify-between gap-4"><div className="flex flex-wrap gap-2 text-[9px] font-semibold tracking-[.2em] opacity-50"><span>{product.category}</span><span>·</span><span>{product.tag}</span></div><WishlistButton slug={product.slug} name={product.name} /></div><h1 className="serif mt-3 text-4xl leading-[1.02] sm:text-5xl md:text-6xl">{product.name}</h1><div className="mt-5 flex items-baseline gap-3"><p className="text-xl sm:text-2xl">${product.price.toFixed(2)}</p>{product.compareAtPrice && <p className="text-sm line-through opacity-40">${product.compareAtPrice.toFixed(2)}</p>}</div><div className="mt-5 flex flex-wrap gap-2 text-[9px] tracking-[.16em]"><span className="border border-[#d8ccc5] px-3 py-2">CURATED ITEM</span><span className="border border-[#d8ccc5] px-3 py-2">{reviewLabel}</span></div><p className="mt-6 max-w-xl text-sm leading-7 opacity-70">{product.description}</p><div className="mt-4 border border-[#e8ded8] bg-white/35 p-4 text-xs leading-6"><strong className="text-[10px] tracking-[.16em]">SIZE NOTE</strong><p className="mt-1 opacity-65">Select your preferred size below. Final fit should be confirmed against the approved sample and supplied size information before launch.</p></div><AddToBag product={product} storeLive={storeLive}/><ProductAccordions details={product.details}/><div className="mt-7 border-y border-[#e8ded8] py-5 text-center"><p className="text-[9px] tracking-[.18em] opacity-50">DELIVERY</p><p className="mt-1 text-[11px]">5–12 BUSINESS DAYS · SHIPPING FROM $7.95 · FREE OVER $75</p></div><div className="mt-6 bg-white/45 p-5 text-[10px] leading-5 opacity-70"><strong className="text-[11px] opacity-100">QUALITY REVIEW</strong><br />{product.qualityStatus === "approved" ? "This item has passed the current sample-review gate for sale." : "Supplier sample verification is required before this item is approved for sale. Product claims are kept conservative until that review is complete."}</div></div></section></div><section className="border-t border-[#e5dad4] py-16 sm:py-20" aria-labelledby="complete-title"><div className="mx-auto max-w-7xl px-4 sm:px-6 md:px-8"><p className="text-[10px] font-semibold tracking-[.28em]">COMPLETE THE LOOK</p><h2 id="complete-title" className="serif mt-2 text-3xl sm:text-4xl">More from {product.category}.</h2><div className="mt-8 grid grid-cols-2 gap-x-3 sm:grid-cols-4 sm:gap-5">{related.map((item) => <ProductCard key={item.slug} product={item}/>)}</div></div></section></main>;
}
